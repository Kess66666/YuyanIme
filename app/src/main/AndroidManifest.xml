<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name="com.yuyan.BaseApplication"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppTheme">
        <service
            android:name="com.yuyan.imemodule.service.ImeService"
            android:exported="true"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>

            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- 权限引导 Activity -->
        <activity
            android:name="com.yuyan.imemodule.PermissionActivity"
            android:exported="true"
            android:theme="@style/Theme.AppTheme" />

        <!-- 主界面 Activity -->
        <activity
            android:name="com.yuyan.imemodule.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 设置向导 Activity -->
        <activity
            android:name="com.yuyan.imemodule.ui.setup.SetupActivity"
            android:exported="true"
            android:theme="@style/Theme.AppTheme" />

        <!-- 设置页面 Activity -->
        <activity
            android:name="com.yuyan.imemodule.ui.activity.SettingsActivity"
            android:exported="true"
            android:theme="@style/Theme.AppTheme" />
    </application>

</manifest>
